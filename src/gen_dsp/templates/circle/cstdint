// cstdint shim for Circle bare-metal builds
// Circle's Rules.mk adds -nostdinc++ which strips C++ stdlib include paths.
// dsp-graph compiled code includes <cstdint> for uint32_t etc.
#ifndef _CIRCLE_CSTDINT_SHIM
#define _CIRCLE_CSTDINT_SHIM
#include <stdint.h>
#endif
